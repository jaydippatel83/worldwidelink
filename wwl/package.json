{
  "name": "wwl",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ wwl",
    "create-local": "graph create --node http://localhost:8020/ wwl",
    "remove-local": "graph remove --node http://localhost:8020/ wwl",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 wwl",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.62.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
